[theme]
base = "dark"
primaryColor = "#00d4aa"

[server]
port = 8501
headless = true
# Bind to loopback only. Without this, Streamlit defaults to 0.0.0.0 and
# anyone on the same Wi-Fi can see the portfolio and click the kill switch.
address = "127.0.0.1"
